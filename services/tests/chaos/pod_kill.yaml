apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: triton-pod-kill
  namespace: voice-detector
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: triton-server
  duration: "1m"
  scheduler:
    cron: "@every 5m"
